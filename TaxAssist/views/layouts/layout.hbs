<!DOCTYPE html>
<html>
  <head>
    <title>{{ projectName }}</title>
      {{# each styles }}
    <link rel='stylesheet' href={{ this }} />
      {{/ each }}
  </head>
  <body>
    {{{ body }}}
  </body>
</html>
